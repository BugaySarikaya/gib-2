import { StrengthPasswordDirective } from './strength-password.directive';

describe('StrengthPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new StrengthPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
